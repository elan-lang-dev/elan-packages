# ELAN Packages Repository

This repository contains **installable libraries for ELAN**.  
You can install packages via the **ELAN package manager** (`el`).

---

## Usage

### Install a package
```bash
el install elangui
el install elangame3d
el install elanocr
...